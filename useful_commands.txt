BASH/COMMAND LINE
`column` - useful for viewing csv from terminal
`column -s, -t file.csv`

`fc` edits last command, `fc -l` lists commands, `fc NUMBER` edits NUMBER command,

`jq -C FILE` jq does json processeing, but this is just pretty printing!
`jq -C . FILE | less -R` - keeps colors in less

Background: finish command with `&` to run in background, or if you forget, ctl-z to suspend, then `bg` to send to background, then `disown`? to not be affected by the terminal closing?

When the "command line freezes" (or even vim?), you probably hit Ctrl-S.
Type Ctrl-Q to get it back

POP OS GUI FREEZE
Ctrl+Alt+F3 for TTY, login, then `pkill -HUP gnome-shell`, then Ctrl+Alt+F2

GIT
Files made locally, want to create a github and local repo and sync:
Create empty repo on github
`cd /path/to/code`
`git init`
`git add STUFF`
`git commit -m "MESSAGE"`
git branch -M main
git remote add origin git@github.com:mlundyCO/REPONAME
git push -u origin main

FILES
lsblk - lists "block devices" useful for seeing all devices and partitions
du - file/directory sizes, use with `-sh` option
rsync - file mover usual syntax `-avh --progress /old/path /new/path`
mkdir - use with `-p` to force creation of entire path

VIM
gg - move cursor to first line.
#G - move cursor to # line.
GG move cursor to last line.
CTRL+f - move cursor Forward full page.
CTRL+b - move cursor Backwards full page.
CTRL+u - move cursor Up half page.
CTRL+d - move cursor Down half page.
zt - move screen so cursor is at Top.
zb - move screen so cursor is at Bottom
zz - center screen on cursor (very useful!)
ZZ - save document and quit (be careful!)
H - move cursor to top ("high up" or "home") of window
M - move to middle of window
L - move to bottom ("low" or "last line") of window
CTL+v for visual block mode, then scroll, then I to insert (useful for commenting)
My config can yank between vims by yanking to system register "+
f,F,t,T, capitalizing changes direction!
q: - puts you in command mode (often get here by accident instead of :q while trying to quit, Ctl-C gets you out.
:sp, :vsp /path/to/file - horizontal and vertical splits, ctrl-w (direction) to move around splits


PYTHON
`grip` is a useful `pip` for viewing markdown locally. Maybe want something that is more universal than python projects. Just `grip FILE.md` then point your browser at the right port.

TMUX
PREFIX then ALT+1 or 2 will split panes evenly horizontally or vertically

VIRTUALBOX
Right-Ctl to uncapture keyboard from VM
Creating new VM:
If you use Debian netinstall's unattended install, your user `vboxuser` will not have sudo access and you need `su -` and use the `vboxuser` passphrase, then `usermod -a -G sudo vboxuser` then log out and in.
`gnome-terminal` is the debian gnome default terminal command if you need to create a keyboard shortcut
Install git and clone `dev_config`.
You can either SSH into the VM, or create keys on the VM and add them to github. Github deploy keys are nice because they are repository specific.
Regardless, setup SSH!
Running Virtualbox Machine: Machine -> Settings -> Network (Expert top left?) -> Port Fowrarding
Create rule
```
Name: ssh
Protocol: TCP
Host IP: 127.0.0.1
Host Port: 2222
Guest IP:
Guest Port: 22
```
install SSH
```
sudo apt update
sudo apt install openssh-server
sudo systemctl enable --now ssh
```
password auth is ok for a local VM right?!
remember ssh keygen looks like `ssh-keygen -t ed25519 -f ~/.ssh/KEYFILENAME -C "KEY COMMENT"

VENV
from the directory to be installed
`python -m venv .venv`

R
Consistent 4-letter prefix naming convention:
d - PDF
p - CDF
q - quantile
r - generates values
Common Distribution root names:
norm, unif, binom, pois, exp, f
